import { environment } from 'src/environments/environment';

export const API_URL = {
    "GET_NEWS_DETAILS": environment.apiBaseUrl + "todos/1",
    "GET_UPDATED_POSTS": environment.apiBaseUrl + "posts",
};